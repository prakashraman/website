import { type NextPage } from "next";
import Head from "next/head";

import { api } from "../utils/api";
import { Outfit } from "@next/font/google";

const font = Outfit({
  subsets: ["latin"],
});

const Home: NextPage = () => {
  const hello = api.example.hello.useQuery({ text: "from tRPC" });
  const text = hello.data ? hello.data.greeting : "Loading tRPC query...";

  return (
    <>
      <Head>
        <title>Prakash Raman - Website</title>
        <meta name="description" content="Generated by create-t3-app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main
        className={["grid grid-cols-[auto_300px]", font.className].join(" ")}
      >
        {/* Left Panel */}
        <div className="pl-[80px]">
          {/* Hero */}
          <section className="flex h-screen items-center border-b border-line">
            <div className="relative flex h-full flex-col justify-center">
              <h1 className="mb-7 text-left text-6xl font-semibold">
                Prakash Raman
              </h1>
              <h3 className="text-2xl text-gray-500">
                As the head of Application Development at Sentiance, my role
                encompasses both architectural planning and hands-on development
                of applications
              </h3>

              <div className="absolute bottom-[80px]">
                <a className="underline">My Highlights</a>
                <a className="ml-5 underline">Try ChatGPT</a>
              </div>
            </div>
          </section>

          {/* Hero */}
          <section className="flex h-screen items-center">
            <div>
              <h1 className="text-left text-6xl font-semibold">about me </h1>
            </div>
          </section>

          {/* Hero */}
          <section className="flex h-screen items-center">
            <div>
              <h1 className="text-left text-6xl font-semibold">chatgpt </h1>
            </div>
          </section>
        </div>

        {/* Right Panel */}
        <div className="w-[300px] border-l border-line">
          right side comes here
        </div>
      </main>
    </>
  );
};

export default Home;
